import React from 'react';
import {makeStyles} from '@material-ui/core/styles';
import Button from '@material-ui/core/Button';

export default function NavButton(props, toggleDrawer) {
  return (
    <div>
      <Button onClick={this.props.toggleDrawer}>Open Right</Button>
    </div>
  );
}
